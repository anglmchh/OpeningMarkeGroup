<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ════════════════════════════════════════════════════════════════════
         MENÚ PRINCIPAL SEPED
         ════════════════════════════════════════════════════════════════════ -->

    <menuitem
        id="menu_seped_root"
        name="SEPED"
        sequence="90"
        web_icon="seped_connector,static/description/icon.png"/>

    <!-- Sub-menú: Sincronización -->
    <menuitem
        id="menu_seped_sync"
        name="Sincronizar"
        parent="menu_seped_root"
        sequence="10"
        action="action_seped_sync_wizard"/>

    <!-- Sub-menú: Configuración -->
    <menuitem
        id="menu_seped_config"
        name="Configuración"
        parent="menu_seped_root"
        sequence="20"
        action="action_seped_config"/>

</odoo>
